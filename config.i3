# Additions to the default I3 configuration supplied by Manjaros.
bindsym $mod+i exec chromium
bindsym --release $mod+c exec slock
# Convenient "e" bindings taken by the default configuration.
bindsym $mod+Shift+m exec emacs
